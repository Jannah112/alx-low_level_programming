#include <stdio.h>
/**
 * main - entry point
 * Return: always 0 (Success)
 */
int main(void)
{
	chat c;

	for (c = 0 ; c <= 9 ; c++)
		putchar(c);
	for (c = 'a' ; a <= 'f' ; c++)
		putchar (c);
	putchar('\n');
	return (0);
}
