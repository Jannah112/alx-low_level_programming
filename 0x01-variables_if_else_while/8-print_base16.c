#include <stdio.h>
/**
 * main - entry point
 * Return: always 0 (Success)
 */
int main(void)
{
	char c;
	int a;

	for (a = 0 ; a <= 9 ; a++)
		putchar(c + '0');
	for (c = 'a' ; c <= 'f' ; c++)
		putchar (c);
	putchar('\n');
	return (0);
}
