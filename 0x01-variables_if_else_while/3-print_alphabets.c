#include <stdio.h>
/**
 * main - entry point
 * Return: always 0 (Success)
 */
int main(void)
{
	int c;
	int d;
	for (c = 'a' ; a <= 'z' ; c++)
		putchar(c);
	for (d = 'A' ; d <= 'Z' ; d++)
	       putchar(d);
	putchar('\n');
	return (0);
}	
