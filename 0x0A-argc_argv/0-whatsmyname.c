#include <stdio.h>
/**
 * main - where to start
 * @argc: number of args
 * @argv: arg vec
 * Return: return 0 or 1
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
